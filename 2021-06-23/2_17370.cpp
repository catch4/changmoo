#include <iostream>
#include <vector>
using namespace std;

const int dx[];
const int dy[];

int n;  // 1~22
int map[50][50];
bool visit[50][50];

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    cin >> n;

    return 0;
}
